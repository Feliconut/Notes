\subsection{Janjigian, HW3}

\begin{enumerate}
    \item \textbf{Problem 1.} Let $B$ be a standard Brownian motion, started from $0$. Let $T_0$ be the first time $B$ hits $0$, i.e. $T_0 = \inf\{t > 0: B_t = 0\}$. Let $R = \inf{t > 1: B_t = 0}$. Using the Markov property, show that
    \begin{equation}
        \mathbb{P}^0(R > 1 + t) = \int_{\mathbb{R}} \frac{1}{\sqrt{2\pi}} e^{-\frac{y^2}{2}} 
        \mathbb{P}^y(T_0 > t) 
        dy.
    \end{equation}
    Then use this to show that $R - 1$ has the pdf
    \begin{equation}
        \frac{1}{\pi \sqrt{t} (1 + t)} 1_{(0, \infty)}(t) d t.
    \end{equation}

    \textbf{Solution.} First, compute the distribution of $T_0$.
        



\end{enumerate}